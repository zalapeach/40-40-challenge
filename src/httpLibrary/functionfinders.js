import { projectsInfo, tasksInfo, userInfo } from './functionsApiCalls';

const dataProjects = projectsInfo();
const datatasks = tasksInfo();
const dataUser = userInfo();

const findTasksId = () => {

    const datatasks = tasksInfo(); 

}